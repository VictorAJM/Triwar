#include "consts.h"
#include "writer.h"

int main()
{
    srand(time(NULL));
    crearCharMatrix();
    ocultarCursor();
    pintar_limites();
    bool game_over = false;
    while (!game_over) {

    }
    return 0;
}